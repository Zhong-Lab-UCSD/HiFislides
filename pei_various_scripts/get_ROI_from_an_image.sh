# ROI: region of interest

# below is one example:
select the region which contain our ROI and copied and saved as a new image (356 × 368 pixels)

# on Mac, installed python 3.10.4 and also opencv
# open the python script by IDLE and give the correct path of the image file (in this case, the 356 x 368 image)
